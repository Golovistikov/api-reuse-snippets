RateLimit:
  description: Rate limit information
  schema:
    type: integer
    format: int64
    example: 1000
Access-Control-Allow-Origin:
  description: CORS support
  schema:
    type: string
    example: '*'
Retry-After:
  description: Time (in seconds) before retry is allowed
  schema:
    type: integer
    format: int32
    example: 30
